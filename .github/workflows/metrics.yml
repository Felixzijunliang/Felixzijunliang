name: Metrics
on:
  # 每天凌晨 0 点更新
  schedule: [{cron: "0 0 * * *"}]
  # 允许你手动点击按钮更新
  workflow_dispatch:
  # 当主分支有 push 操作时更新
  push: {branches: ["master", "main"]}
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          user: Felixzijunliang
          template: classic
          
          # 【核心修改 1】基础设置：只保留头部和仓库
          base: header, repositories
          config_timezone: Asia/Shanghai
          
          # 【核心修改 2】语言显示：过滤掉 HTML/CSS 等非代码语言
          plugin_languages: yes
          plugin_languages_ignored: >-
            html, css, tex, less, dockerfile, makefile, qmake, lex, cmake, gnuplot
          plugin_languages_limit: 8
          
          # 【核心修改 3】折线图：保留 Contribution 趋势
          plugin_lines: yes
          
          # 【👉 新增功能】流量统计 (Traffic)
          # 这会显示你所有仓库在过去 14 天内的总浏览量和克隆量
          plugin_traffic: yes
