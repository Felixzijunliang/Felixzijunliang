name: Metrics
on:
  # 每天凌晨 0 点更新
  schedule: [{cron: "0 0 * * *"}]
  # 允许你手动点击按钮更新
  workflow_dispatch:
  # 当主分支有 push 操作时更新
  push: {branches: ["master", "main"]}
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          user: Felixzijunliang
          template: classic
          
          # 基础设置
          base: header, repositories
          config_timezone: Asia/Shanghai
          
          # 语言设置
          plugin_languages: yes
          plugin_languages_ignored: html, css, tex, less, dockerfile, makefile, qmake, lex, cmake, gnuplot
          plugin_languages_limit: 8
          
          # 贡献趋势图
          plugin_lines: yes
          
          # 【👇 重点修改这里】流量统计设置
          plugin_traffic: yes
          plugin_traffic_clones: yes      # 强制显示克隆数
          plugin_traffic_clones_days: 14  # 统计过去14天
          plugin_traffic_views: yes       # 强制显示浏览数
          plugin_traffic_views_days: 14   # 统计过去14天
